### What is this ? 📂
docker compose file to launch an n8n service

### How to use this ?

1. Copy the both yml files in a directory of your computer
2. Navigate to the directory containing the yml file(s).
3. Run `docker-compose up -d`
4. Open _`localhost:5678`_ to access n8n.
5. To stop the stack run `docker compose down`
